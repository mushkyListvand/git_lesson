add one child
shmuel listvands
change for stash
feature awesome
def my_square(y):
        """takes a value and returns the squared value.
           uses the *  operator 
        """
        return(y**2)

second child been added
print(my_square(24))
ptint(my_square(13))
print(my_square2(42))
commit 1 for revert
